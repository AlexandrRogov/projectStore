package com.gmail.rogov.controller.util;

import com.gmail.rogov.service.model.UserDTO;
import com.gmail.rogov.controller.model.UserPrincipal;

public class UserPrincipalConverter {

    public static UserPrincipal toUserPrincipal(UserDTO user) {
//        return UserPrincipal.newBuilder()
//                .id(user.getId())
////                .email(user.)
//                .name(user.getFirstName() + " " + user.getLastName())
//                .role(user.getRoleId())
//                .build();
        return null;
    }
}
